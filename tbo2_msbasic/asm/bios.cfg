MEMORY {
  ZP: start = $0000, size = $100, type = rw;
  INPUT_BUFFER: start = $0300, size = $100, type = rw, file = "";
  ROM: start = $8000, size = $7F00, type = ro, fill = yes, file = %O;
  WOZMON: start = $FF00, size = $FA, type = ro, fill = yes, file = %O;
  RESETVEC: start = $FFFA, size = 6, type = ro, fill = yes, file = %O;
}

SEGMENTS {
  ZEROPAGE: load = ZP, type = zp;
  INPUT_BUFFER: load = INPUT_BUFFER, type = rw;
  BIOS: load = "ROM", type = ro;
  WOZMON: load = "WOZMON", type = ro;
  RESETVEC: load = "RESETVEC", type = ro;
}
